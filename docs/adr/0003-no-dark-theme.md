# ダークテーマを持たない

このサイトはダークテーマを持たない。白を基調にした見た目がサイトの性格そのものであり、色を反転させると別のサイトになるため。

## 帰結

- `.dark` クラスは定義しない。`next-themes` は4色のカラーテーマ(default / blue / green / pink)の切り替えにだけ使う。
- shadcn/ui が生成するコンポーネントには `dark:` を含むクラスが付いてくる。定義がないので何も効かない。**追加したら削る。**
